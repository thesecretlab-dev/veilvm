# Local stack revive 2026-08-27

Not Fuji. Same chain IDs as 2026-08-24.

## What was wrong

AvalancheGo was still running (`:9660` HTTP 503). Health:

```
proposervm height index (77383) should never be lower than the inner height index (77384)
```

Unclean shutdown. Anvil, router, relayer were down.

## What we did

- Stopped avalanchego / plugin
- Moved corrupt `nodedata/chainData/bdRGUMA7…` to `.local/nodedata-chain-corrupt-20260827`
- Restarted `scripts/start-local-daemon.ps1` on the **same** subnet/chain IDs
- Redeployed anvil rails (LocalTeleporter mock)
- Relayer one-shot now skips already-EXECUTED intents and mailbox misses (so e2e is not poisoned by leftover logs)
- `local-stack-e2e.mjs` isolates order vs liquidity mailbox env

## Checks 2026-08-27

| Surface | Result |
|---|---|
| `:9660` health | `healthy=true` |
| AMM smoke | PASS |
| `check-companion-primitives` | PASS (local-mock; WARN feeConfigManager, multicall3) |
| `local-stack-e2e` | PASS (order + liq EXECUTED) |
| `top-to-bottom` | PASS |
| `privacy-loop` | PASS (commit/reveal/proof/clear). D06 gossip still not in binary |

Fuji L1 still closed until explicitly authorized.
