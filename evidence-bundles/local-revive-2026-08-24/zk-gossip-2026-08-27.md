# ZK / gossip 2026-08-27

D06 was FAIL because encrypted tx gossip was not in the binary (the Feb runbook named files that were never in this tree).

## Now in the binary

- `hypersdk/chain/transaction_marshaller_encrypted.go`: gossip payloads wrap as `VTG1` + AES-256-GCM.
- Fail-closed when `vm.txGossipEncryptionRequired=true` and key missing, or when plaintext gossip arrives.
- Local chain config enables it. Key lives in gitignored `.local/tx-gossip.key` (32-byte hex).
- Unit tests: roundtrip, plaintext rejected, wrong key fails, empty required key fails.

## Still not done

- Threshold committee decrypt (t-of-n, Shamir/X25519) is not implemented. One local validator cannot be that.
- Shielded-ledger Groth16 still binds a canonical SHA256 preimage; it does not encode matching/nullifier/balance constraints inside the circuit.
- Do not claim a private mempool or whitepaper-complete ZK validity.

## Checks

- `go test ./chain -run Gossip` PASS
- Plugin rebuilt; node healthy with `txGossipEncryptionRequired=true` and 32-byte key
- `privacy-loop` PASS (commit/reveal/groth16/clear, proveMs=603)
